@model PlayersListViewModel
@foreach (var p in Model.Players) {
	<div class="card card-outline-primary m-1 p-l">
<div class="bg-faded p-l">
<h4>
@p.Id  @p.Name
<span class="badge badge-pill badge-primary" style="float:right">
<small>@p.Position</small>
</span>
</h4>
</div>
<div class= "card-text p-l">@p.RedCard
@p.TeamId</div>
</div>


<div>
</div>
}
<div page-model="@Model.Paginginfo" page-action="List" page-classes-enabled="true"
page-class="btn" page-class-normal="btn-secondary"
page-class-selected="btn-primary" class="btn-group pull-right m-l">
</div>

@*@foreach (var p in Model) {
<div>IEnumerable<Player>

@p.Id
@p.Name
@p.Position

</div>
}
@*

@foreach(var thisLink in Model)
{
<div>





<h3>@thisLink.Name</h3>
</div>
}





<h3>@p.Position</h3>
<h3>@p.TeamId</h3>(int)
<h3>@p.RedCard</h3>
<h3>@p.Id</h3>

@model IEnumerable<Phone>
@foreach (var p in Model) {
<div>
<h3>@p.Id</h3>
@p.Name
<h4>@p.Company.ToString()</h4>
</div>
}*@