@model GameListViewModel
<div class="panel-body">
<h2>Here is the list of games</h2>


<table class="table table-sm table-striped table-bordered">
<thead>
<tr>

<th>Id</th>
<th>Score</th>
<th>Data</th>
<th>First Team</th>
<th>Second Team</th>
</tr>
</thead>
<tbody>



@*<div class="card card-outline-primary m-1 p-l"></div>IEnumerable<Game>*@
@*@Model.CurrentTeamName
*@

@foreach (var pp in Model.Games) {
<tr>
@*<td>@p.Id</td>
<td>@p.Score</td>
<td>@p.Data.Date.ToString("ddd, dd MMM yyy")</td>*@
@*@Html.Partial("GameSummary", pp)
*@
<td>@pp.Id</td>
<td>@pp.Score</td>
<td>@pp.Data.Date.ToString("ddd, dd MMM yyy")</td>
@*<td>@pp.FirstTeamId</td>
<td>@pp.SecondTeamId</td>*@
<td>@Model.Teams.ToList().Find(x => x.Id==pp.FirstTeamId).Name</td>
<td>@Model.Teams.ToList().Find(x => x.Id==pp.SecondTeamId).Name</td>
</tr>}
@*@Html.Partial("PlayerSummary", p)*@





</tbody>
</table>
</div>

<div page-model="@Model.Paginginfo" page-action="List" page-classes-enabled="true"
page-class="btn" page-class-normal="btn-secondary"
page-class-selected="btn-primary" page-class-selected="btn-primary" page-url-firstteam="@Model.CurrentTeam"
class="btn-group pull-right m-l">

</div>@**@

@*@foreach (var p in Model) {p.TeamId.Id.Contains(" ")
<div>IEnumerable<Player>Async

@p.Id
@p.Name
@p.Position

</div>
}
@*

@foreach(var thisLink in Model)
{
<div>





<h3>@thisLink.Name</h3>
</div>
}





<h3>@p.Position</h3>
<h3>@p.TeamId</h3>(int)
<h3>@p.RedCard</h3>
<h3>@p.Id</h3>

@model IEnumerable<Phone>
@foreach (var p in Model) {
<div>
<h3>@p.Id</h3>
@p.Name
<h4>@p.Company.ToString()</h4>
</div>
}*@